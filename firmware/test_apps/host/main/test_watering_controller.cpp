// SPDX-FileCopyrightText: 2026 Cryptotomte
// SPDX-License-Identifier: AGPL-3.0-or-later
/**
 * @file test_watering_controller.cpp
 * @brief Host suite for the pure WateringController (feature 011, US1/US3).
 *
 * The automatic + soak-gate + fail-safe branches (US1) and the manual
 * override + data-logging branches (US3) are added here in later tasks
 * (T006/T007/T011). This file registers the suite entry point now so the
 * host test app links and runs empty until those tests land.
 */

#include "unity.h"

// TODO(T006/T007/T011): register the WateringController tests here.
void run_watering_controller_tests(void)
{
}
