# Board abstraction component (header-only).
# Pin mappings and board feature flags selected via Kconfig
# (CONFIG_BOARD_REV1_DEVKIT / CONFIG_BOARD_REV2, defined in main/Kconfig.projbuild).
idf_component_register(INCLUDE_DIRS "include")
