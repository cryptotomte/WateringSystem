# Managed dependencies (IDF Component Registry), pinned for reproducible builds.
# Used by app_main via components/sensors since feature 004. Keep the esp-modbus
# version in lockstep with components/sensors/idf_component.yml (both pin
# ==2.1.2; bump together or the resolver conflicts).
dependencies:
  idf: ">=6.0.0"
  espressif/esp-modbus: "==2.1.2"
  joltwallet/littlefs: "==1.22.1"
